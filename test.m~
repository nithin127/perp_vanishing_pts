suitable_set = [vp_candidates(2),vp_candidates(4)];
vp_1 = vp_candidates(1);
i = vp_candidates(2);
j = vp_candidates(4);
%vanishing_lines_criteria(i,j,vp_1,lines,intn_pts,vote_matrix,grayIm)

a_thres = 5*pi/180;
d_thres = 30;

[res,o,f] = orthogonality_criteria(i,j,k,pts,size_im,lines)

